CONFIG_ENV=config.env

# 1. 核心源码配置（路径已验证，与mt6873-dev完全对齐）
KERNEL_SOURCE=https://github.com/mt6873-dev/android_kernel_xiaomi_mt6885
KERNEL_SOURCE_BRANCH=cgroup-v2
KERNEL_CONFIG=arch/arm64/configs/vendor/atom_user_defconfig
KERNEL_IMAGE_NAME=Image.gz-dtb
ARCH=arm64
ADD_LOCALVERSION_TO_FILENAME=false
EXTRA_CMDS=LD=ld.lld

# 2. 编译工具链（核心修复：解决git认证+Clang下载问题）
## 方案：改用【匿名HTTPS git地址】绕过认证，同时保留原版本
USE_CUSTOM_CLANG=true
# 增加 anonymous:@ 前缀，实现匿名clone，解决"could not read Username"报错
CUSTOM_CLANG_SOURCE=https://anonymous:@github.com/android/prebuilts_clang_host_linux-x86
CUSTOM_CLANG_BRANCH=clang-r487747c
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

## （备用）若仍失败，可切换为稳定版AOSP Clang（r450784e）
# USE_CUSTOM_CLANG=false
# CLANG_BRANCH=master-kernel-build-2021
# CLANG_VERSION=r450784e

# 3. GCC配置（保持不变，适配32位工具链）
ENABLE_GCC_ARM64=false
ENABLE_GCC_ARM32=true
USE_CUSTOM_GCC_64=false
CUSTOM_GCC_64_SOURCE=
CUSTOM_GCC_64_BRANCH=
CUSTOM_GCC_64_BIN=aarch64-linux-android-
USE_CUSTOM_GCC_32=false
CUSTOM_GCC_32_SOURCE=
CUSTOM_GCC_32_BRANCH=
CUSTOM_GCC_32_BIN=arm-linux-androideabi-

# 4. SukiSU-Ultra核心配置（已验证，版本锁定）
ENABLE_KERNELSU=true
KERNELSU_GIT=https://github.com/SukiSU-Ultra/SukiSU-Ultra.git
KERNELSU_TAG=86e6fec
KSU_EXPECTED_SIZE=
KSU_EXPECTED_HASH=

# 5. 内核自动适配配置（与mt6873-dev适配逻辑一致）
DISABLE_LTO=false
DISABLE_CC_WERROR=true
ADD_KPROBES_CONFIG=true
ADD_OVERLAYFS_CONFIG=true
APPLY_KSU_PATCH=true
REMOVE_UNUSED_PACKAGES=true

# 6. AnyKernel3卡刷包配置（保持不变）
USE_CUSTOM_ANYKERNEL3=false
CUSTOM_ANYKERNEL3_SOURCE=
CUSTOM_ANYKERNEL3_BRANCH=

# 7. 编译加速配置（保持不变）
ENABLE_CCACHE=true

# 8. 设备专属配置（适配mt6873平台DTBO）
NEED_DTBO=true
BUILD_BOOT_IMG=false
# SOURCE_BOOT_IMAGE=https://raw.githubusercontent.com/xiaoleGun/KernelSU_action/main/boot/boot.img
